# RAG 完整流程实战

## RAG 系统的完整架构

```
┌─────────────────────────────────────────────────────────┐
│                    RAG 系统架构                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  离线阶段（准备知识库）                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 文档加载  │→│ 文本分块  │→│ 向量化   │              │
│  └──────────┘  └──────────┘  └──────────┘              │
│                                    ↓                     │
│                            ┌──────────────┐             │
│                            │ 向量数据库    │             │
│                            └──────────────┘             │
│                                                          │
│  在线阶段（用户提问）                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 用户提问  │→│ 向量检索  │→│ AI 生成  │→ 返回答案    │
│  └──────────┘  └──────────┘  └──────────┘              │
│                      ↑                                   │
│                ┌──────────────┐                         │
│                │ 向量数据库    │                         │
│                └──────────────┘                         │
└─────────────────────────────────────────────────────────┘
```

---

## 完整的 RAG 系统实现

### 1. 项目结构

```
rag_system/
├── data/                    # 原始文档
│   ├── doc1.pdf
│   ├── doc2.md
│   └── doc3.txt
├── chroma_db/              # 向量数据库（自动生成）
├── src/
│   ├── document_loader.py  # 文档加载
│   ├── text_processor.py   # 文本处理
│   ├── vector_store.py     # 向量存储
│   ├── retriever.py        # 检索器
│   └── rag_pipeline.py     # 完整 Pipeline
├── main.py                 # 主程序
└── requirements.txt        # 依赖
```

### 2. 依赖安装

```bash
pip install chromadb openai langchain langchain-community pypdf2 python-docx beautifulsoup4
```

`requirements.txt`:
```
chromadb>=0.4.0
openai>=1.0.0
langchain>=0.1.0
langchain-community>=0.0.1
pypdf2>=3.0.0
python-docx>=0.8.11
beautifulsoup4>=4.12.0
```

### 3. 完整代码实现

#### document_loader.py

```python
"""文档加载模块"""
import os
from typing import List, Dict
import PyPDF2
from docx import Document
from bs4 import BeautifulSoup

class DocumentLoader:
    """文档加载器"""
    
    @staticmethod
    def load_file(file_path: str) -> Dict:
        """
        加载单个文件
        
        Returns:
            Dict: {'text': str, 'metadata': dict}
        """
        ext = os.path.splitext(file_path)[1].lower()
        
        if ext == '.txt' or ext == '.md':
            text = DocumentLoader._load_text(file_path)
        elif ext == '.pdf':
            text = DocumentLoader._load_pdf(file_path)
        elif ext in ['.doc', '.docx']:
            text = DocumentLoader._load_word(file_path)
        elif ext in ['.html', '.htm']:
            text = DocumentLoader._load_html(file_path)
        else:
            raise ValueError(f"不支持的文件格式: {ext}")
        
        return {
            'text': text,
            'metadata': {
                'source': file_path,
                'filename': os.path.basename(file_path),
                'extension': ext
            }
        }
    
    @staticmethod
    def load_directory(dir_path: str) -> List[Dict]:
        """加载目录下的所有文档"""
        documents = []
        
        for root, dirs, files in os.walk(dir_path):
            for file in files:
                file_path = os.path.join(root, file)
                try:
                    doc = DocumentLoader.load_file(file_path)
                    documents.append(doc)
                    print(f"✓ 加载: {file}")
                except Exception as e:
                    print(f"✗ 跳过: {file} ({e})")
        
        return documents
    
    @staticmethod
    def _load_text(file_path: str) -> str:
        with open(file_path, 'r', encoding='utf-8') as f:
            return f.read()
    
    @staticmethod
    def _load_pdf(file_path: str) -> str:
        text = ""
        with open(file_path, 'rb') as f:
            reader = PyPDF2.PdfReader(f)
            for page in reader.pages:
                text += page.extract_text() + "\n"
        return text
    
    @staticmethod
    def _load_word(file_path: str) -> str:
        doc = Document(file_path)
        return "\n".join([para.text for para in doc.paragraphs])
    
    @staticmethod
    def _load_html(file_path: str) -> str:
        with open(file_path, 'r', encoding='utf-8') as f:
            soup = BeautifulSoup(f.read(), 'html.parser')
            for script in soup(["script", "style"]):
                script.decompose()
            return soup.get_text()
```

#### text_processor.py

```python
"""文本处理模块"""
import re
import hashlib
from typing import List, Dict

class TextProcessor:
    """文本处理器"""
    
    def __init__(self, chunk_size: int = 500, overlap: int = 50):
        self.chunk_size = chunk_size
        self.overlap = overlap
    
    def process(self, document: Dict) -> List[Dict]:
        """
        处理文档：清洗 + 分块 + 添加元数据
        
        Args:
            document: {'text': str, 'metadata': dict}
        
        Returns:
            List[Dict]: 处理后的文本块列表
        """
        # 1. 清洗文本
        text = self._clean_text(document['text'])
        
        # 2. 分块
        chunks = self._chunk_text(text)
        
        # 3. 添加元数据
        processed_chunks = []
        for i, chunk in enumerate(chunks):
            chunk_id = self._generate_chunk_id(
                document['metadata']['source'], i
            )
            
            processed_chunks.append({
                'text': chunk,
                'metadata': {
                    **document['metadata'],
                    'chunk_id': chunk_id,
                    'chunk_index': i,
                    'total_chunks': len(chunks),
                    'chunk_size': len(chunk)
                }
            })
        
        return processed_chunks
    
    def _clean_text(self, text: str) -> str:
        """清洗文本"""
        # 移除多余空格
        text = re.sub(r'\s+', ' ', text)
        # 移除 HTML 标签
        text = re.sub(r'<[^>]+>', '', text)
        # 去除首尾空格
        text = text.strip()
        return text
    
    def _chunk_text(self, text: str) -> List[str]:
        """分块（带重叠）"""
        chunks = []
        start = 0
        
        while start < len(text):
            end = start + self.chunk_size
            chunk = text[start:end]
            
            # 如果不是最后一块，尝试在句子边界切分
            if end < len(text):
                # 找最后一个句号
                last_period = chunk.rfind('。')
                if last_period > self.chunk_size * 0.5:  # 至少保留一半
                    chunk = chunk[:last_period + 1]
                    end = start + last_period + 1
            
            chunks.append(chunk)
            start += self.chunk_size - self.overlap
        
        return chunks
    
    @staticmethod
    def _generate_chunk_id(file_path: str, chunk_index: int) -> str:
        """生成唯一的 chunk ID"""
        file_hash = hashlib.md5(file_path.encode()).hexdigest()[:8]
        return f"{file_hash}_chunk_{chunk_index}"
```

#### vector_store.py

```python
"""向量存储模块"""
import chromadb
from typing import List, Dict

class VectorStore:
    """向量存储"""
    
    def __init__(self, db_path: str = "./chroma_db", collection_name: str = "documents"):
        self.client = chromadb.PersistentClient(path=db_path)
        self.collection = self.client.get_or_create_collection(
            name=collection_name,
            metadata={"hnsw:space": "cosine"}
        )
    
    def add_chunks(self, chunks: List[Dict], batch_size: int = 100):
        """
        批量添加文本块
        
        Args:
            chunks: List[{'text': str, 'metadata': dict}]
            batch_size: 批处理大小
        """
        total = len(chunks)
        
        for i in range(0, total, batch_size):
            batch = chunks[i:i+batch_size]
            
            texts = [chunk['text'] for chunk in batch]
            metadatas = [chunk['metadata'] for chunk in batch]
            ids = [chunk['metadata']['chunk_id'] for chunk in batch]
            
            self.collection.add(
                documents=texts,
                metadatas=metadatas,
                ids=ids
            )
            
            print(f"已添加 {min(i+batch_size, total)}/{total} 个文本块")
    
    def search(self, query: str, top_k: int = 3, filter_dict: Dict = None) -> Dict:
        """
        搜索相关文档
        
        Args:
            query: 查询文本
            top_k: 返回结果数量
            filter_dict: 过滤条件
        
        Returns:
            Dict: {'documents': List[str], 'metadatas': List[dict], 'distances': List[float]}
        """
        results = self.collection.query(
            query_texts=[query],
            n_results=top_k,
            where=filter_dict
        )
        
        return {
            'documents': results['documents'][0],
            'metadatas': results['metadatas'][0],
            'distances': results['distances'][0]
        }
    
    def count(self) -> int:
        """获取文档数量"""
        return self.collection.count()
    
    def clear(self):
        """清空集合"""
        self.client.delete_collection(self.collection.name)
        self.collection = self.client.create_collection(
            name=self.collection.name,
            metadata={"hnsw:space": "cosine"}
        )
```

#### retriever.py

```python
"""检索模块"""
from typing import List, Dict
from openai import OpenAI

class Retriever:
    """检索器"""
    
    def __init__(self, vector_store, llm_client: OpenAI):
        self.vector_store = vector_store
        self.llm_client = llm_client
    
    def retrieve_and_generate(
        self, 
        query: str, 
        top_k: int = 3,
        model: str = "gpt-4o-mini"
    ) -> Dict:
        """
        检索 + 生成答案
        
        Returns:
            Dict: {
                'answer': str,
                'sources': List[dict],
                'retrieved_docs': List[str]
            }
        """
        # 1. 检索相关文档
        search_results = self.vector_store.search(query, top_k=top_k)
        
        if not search_results['documents']:
            return {
                'answer': "抱歉，我没有找到相关信息。",
                'sources': [],
                'retrieved_docs': []
            }
        
        # 2. 构造 Prompt
        context = self._build_context(search_results['documents'])
        prompt = self._build_prompt(query, context)
        
        # 3. 调用 LLM 生成答案
        response = self.llm_client.chat.completions.create(
            model=model,
            messages=[
                {"role": "system", "content": "你是一个有帮助的助手，请基于提供的文档回答问题。"},
                {"role": "user", "content": prompt}
            ],
            temperature=0.3
        )
        
        answer = response.choices[0].message.content
        
        # 4. 整理来源信息
        sources = self._extract_sources(search_results['metadatas'])
        
        return {
            'answer': answer,
            'sources': sources,
            'retrieved_docs': search_results['documents']
        }
    
    def retrieve_and_generate_stream(
        self,
        query: str,
        top_k: int = 3,
        model: str = "gpt-4o-mini"
    ):
        """流式输出版本"""
        # 1. 检索
        search_results = self.vector_store.search(query, top_k=top_k)
        
        if not search_results['documents']:
            yield "抱歉，我没有找到相关信息。"
            return
        
        # 2. 构造 Prompt
        context = self._build_context(search_results['documents'])
        prompt = self._build_prompt(query, context)
        
        # 3. 流式生成
        stream = self.llm_client.chat.completions.create(
            model=model,
            messages=[
                {"role": "system", "content": "你是一个有帮助的助手，请基于提供的文档回答问题。"},
                {"role": "user", "content": prompt}
            ],
            temperature=0.3,
            stream=True
        )
        
        for chunk in stream:
            if chunk.choices[0].delta.content:
                yield chunk.choices[0].delta.content
    
    @staticmethod
    def _build_context(documents: List[str]) -> str:
        """构造上下文"""
        context = ""
        for i, doc in enumerate(documents, 1):
            context += f"\n文档 {i}:\n{doc}\n"
        return context
    
    @staticmethod
    def _build_prompt(query: str, context: str) -> str:
        """构造 Prompt"""
        return f"""请根据以下文档回答问题。

文档内容：
{context}

问题：{query}

要求：
1. 只基于文档内容回答，不要编造信息
2. 如果文档中没有相关信息，请明确说明
3. 回答要准确、简洁
4. 如果可能，请引用文档中的原文

回答："""
    
    @staticmethod
    def _extract_sources(metadatas: List[Dict]) -> List[Dict]:
        """提取来源信息"""
        sources = []
        seen = set()
        
        for meta in metadatas:
            source = meta.get('source', 'unknown')
            if source not in seen:
                sources.append({
                    'source': source,
                    'filename': meta.get('filename', 'unknown')
                })
                seen.add(source)
        
        return sources
```

#### rag_pipeline.py

```python
"""RAG Pipeline 主模块"""
from typing import List
from openai import OpenAI
from .document_loader import DocumentLoader
from .text_processor import TextProcessor
from .vector_store import VectorStore
from .retriever import Retriever

class RAGPipeline:
    """完整的 RAG Pipeline"""
    
    def __init__(
        self,
        openai_api_key: str,
        db_path: str = "./chroma_db",
        collection_name: str = "documents",
        chunk_size: int = 500,
        overlap: int = 50
    ):
        # 初始化各个组件
        self.loader = DocumentLoader()
        self.processor = TextProcessor(chunk_size=chunk_size, overlap=overlap)
        self.vector_store = VectorStore(db_path=db_path, collection_name=collection_name)
        
        # 初始化 LLM 客户端
        self.llm_client = OpenAI(api_key=openai_api_key)
        
        # 初始化检索器
        self.retriever = Retriever(self.vector_store, self.llm_client)
    
    def ingest_directory(self, dir_path: str):
        """
        摄入整个目录的文档
        
        流程：加载 → 处理 → 存储
        """
        print(f"\n开始处理目录: {dir_path}")
        print("=" * 50)
        
        # 1. 加载文档
        print("\n[1/3] 加载文档...")
        documents = self.loader.load_directory(dir_path)
        print(f"✓ 成功加载 {len(documents)} 个文档")
        
        # 2. 处理文档
        print("\n[2/3] 处理文档（清洗 + 分块）...")
        all_chunks = []
        for doc in documents:
            chunks = self.processor.process(doc)
            all_chunks.extend(chunks)
        print(f"✓ 生成 {len(all_chunks)} 个文本块")
        
        # 3. 存储到向量数据库
        print("\n[3/3] 存储到向量数据库...")
        self.vector_store.add_chunks(all_chunks)
        print(f"✓ 完成！数据库中共有 {self.vector_store.count()} 个文本块")
    
    def query(self, question: str, top_k: int = 3) -> dict:
        """
        查询
        
        Args:
            question: 用户问题
            top_k: 检索文档数量
        
        Returns:
            Dict: {'answer': str, 'sources': List[dict], 'retrieved_docs': List[str]}
        """
        return self.retriever.retrieve_and_generate(question, top_k=top_k)
    
    def query_stream(self, question: str, top_k: int = 3):
        """流式查询"""
        return self.retriever.retrieve_and_generate_stream(question, top_k=top_k)
    
    def get_stats(self) -> dict:
        """获取统计信息"""
        return {
            'total_chunks': self.vector_store.count()
        }
```

#### main.py

```python
"""主程序"""
import os
from src.rag_pipeline import RAGPipeline

def main():
    # 配置
    OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "your-api-key")
    DATA_DIR = "./data"
    
    # 初始化 RAG Pipeline
    print("初始化 RAG Pipeline...")
    pipeline = RAGPipeline(
        openai_api_key=OPENAI_API_KEY,
        db_path="./chroma_db",
        collection_name="my_knowledge_base",
        chunk_size=500,
        overlap=50
    )
    
    # 摄入文档（只需要执行一次）
    if input("是否需要重新摄入文档？(y/n): ").lower() == 'y':
        pipeline.ingest_directory(DATA_DIR)
    
    # 查看统计
    stats = pipeline.get_stats()
    print(f"\n数据库统计: {stats['total_chunks']} 个文本块")
    
    # 交互式问答
    print("\n" + "=" * 50)
    print("RAG 系统已就绪！输入 'quit' 退出")
    print("=" * 50)
    
    while True:
        question = input("\n你的问题: ").strip()
        
        if question.lower() in ['quit', 'exit', 'q']:
            break
        
        if not question:
            continue
        
        # 查询
        print("\n思考中...")
        result = pipeline.query(question, top_k=3)
        
        # 输出答案
        print("\n" + "=" * 50)
        print("回答:")
        print(result['answer'])
        
        # 输出来源
        if result['sources']:
            print("\n来源:")
            for i, source in enumerate(result['sources'], 1):
                print(f"{i}. {source['filename']}")
        
        print("=" * 50)

if __name__ == "__main__":
    main()
```

---

## 使用示例

### 1. 准备数据

```bash
mkdir data
# 把你的文档放到 data/ 目录下
```

### 2. 运行程序

```bash
# 设置 API Key
export OPENAI_API_KEY="your-api-key"

# 运行
python main.py
```

### 3. 交互示例

```
初始化 RAG Pipeline...
是否需要重新摄入文档？(y/n): y

开始处理目录: ./data
==================================================

[1/3] 加载文档...
✓ 加载: python_tutorial.pdf
✓ 加载: fastapi_guide.md
✓ 成功加载 2 个文档

[2/3] 处理文档（清洗 + 分块）...
✓ 生成 45 个文本块

[3/3] 存储到向量数据库...
已添加 45/45 个文本块
✓ 完成！数据库中共有 45 个文本块

数据库统计: 45 个文本块

==================================================
RAG 系统已就绪！输入 'quit' 退出
==================================================

你的问题: 如何使用 FastAPI 创建路由？

思考中...

==================================================
回答:
使用 FastAPI 创建路由非常简单。首先导入 FastAPI，然后创建应用实例，
使用装饰器定义路由：

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
    return {"message": "Hello World"}
```

来源:
1. fastapi_guide.md
==================================================
```

---

## 性能优化

### 1. 批量处理

```python
# 批量添加文档
self.vector_store.add_chunks(all_chunks, batch_size=100)
```

### 2. 缓存检索结果

```python
from functools import lru_cache

@lru_cache(maxsize=100)
def cached_search(query: str, top_k: int):
    return self.vector_store.search(query, top_k)
```

### 3. 异步处理

```python
import asyncio

async def ingest_directory_async(self, dir_path: str):
    """异步摄入文档"""
    # 实现异步版本
    pass
```

---

## 总结

这是一个完整的、生产级的 RAG 系统实现，包含：

1. **文档加载**：支持多种格式
2. **文本处理**：清洗 + 智能分块
3. **向量存储**：Chroma 数据库
4. **检索生成**：向量检索 + LLM 生成
5. **流式输出**：提升用户体验

你可以基于这个框架：
- 添加更多文档格式支持
- 实现混合检索（向量 + 关键词）
- 添加重排序（Rerank）
- 实现多轮对话记忆
- 用 FastAPI 包装成 Web 服务

这就是一个完整的 RAG 系统！
